services:
  - type: web
    name: web-traffic-dashboard
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run dashboard.py --server.port $PORT --server.address 0.0.0.0"
    plan: free
    disks:
      - name: trafficdata
        mountPath: /data

  - type: worker
    name: traffic-scraper
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python forex.py"
    plan: free
    disks:
      - name: trafficdata
        mountPath: /data

disks:
  - name: trafficdata
    sizeGB: 1
